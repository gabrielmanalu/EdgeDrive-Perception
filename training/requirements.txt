# EdgeDrive-Perception — Training Requirements
# Install with: pip install -r requirements.txt
#
# Tested on:
#   Python     3.12
#   CUDA       12.8
#   GPU        Tesla T4 (Google Colab)
#
# Notes:
#   - TFLite export (quantize.py) requires tensorflow which is auto-installed
#     by Ultralytics during the first export call. No manual install needed.
#   - TensorRT export must be run on Jetson hardware, not here.
#     See deployment/ for Jetson-side requirements.
#   - PointPillars (fusion/) requires a separate environment with
#     PyTorch 2.1.0 + mmcv 2.1.0. See fusion/README.md.

# ── Deep learning framework ───────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0

# ── YOLO models and training ──────────────────────────────────────────────────
ultralytics>=8.4.0          # YOLO26, YOLOv8, Solutions API, export pipeline

# ── ONNX export ───────────────────────────────────────────────────────────────
onnx>=1.12.0,<2.0.0         # model graph export
onnxslim>=0.1.71            # ONNX graph optimization/simplification
onnxruntime-gpu>=1.16.0     # ONNX inference validation

# ── TFLite export (INT8 + FP16) ───────────────────────────────────────────────
# These are auto-installed by Ultralytics on first TFLite export.
# Listed here for reproducibility.
onnx2tf>=1.26.3,<1.29.0     # ONNX → TensorFlow → TFLite conversion
ai-edge-litert>=1.2.0       # TFLite runtime
onnx-graphsurgeon>=0.3.26   # ONNX graph manipulation
sng4onnx>=1.0.1             # ONNX node name generator

# ── Structured pruning (experimental) ────────────────────────────────────────
# torch-pruning was evaluated for structured channel pruning but could not
# trace YOLO26n's custom blocks (C3k2, C2PSA, SPPF). Listed for reference.
# torch-pruning>=1.3.0

# ── nuScenes dataset ──────────────────────────────────────────────────────────
nuscenes-devkit>=1.1.11     # dataset loading, calibration, visualization

# ── Geometry and math ─────────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0               # ConvexHull for segmentation label generation
pyquaternion>=0.9.9         # 3D rotation for coordinate transforms

# ── Image processing ──────────────────────────────────────────────────────────
opencv-python>=4.8.0

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0